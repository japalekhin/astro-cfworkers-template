---
import "@/styles/global.css";
import SiteHeader from "@/components/site-header.astro";
import SiteDrawer from "@/components/site-drawer.astro";
import SiteFooter from "@/components/site-footer.astro";
import { THEME_COOKIE_NAME } from "@/shared/definitions/theme";

export interface Props {
  title: string;
}

const { title } = Astro.props;
const currentTheme = Astro.cookies.get(THEME_COOKIE_NAME)?.value === "dark" ? "dark" : "light";
---

<html lang="en" data-theme={currentTheme}>
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>
  <body>
    <div class="drawer min-h-screen">
      <input id="site-drawer" type="checkbox" class="drawer-toggle" />
      <main class="drawer-content flex flex-col min-h-screen">
        <SiteHeader />
        <div class="grow">
          <slot />
        </div>
        <SiteFooter />
      </main>
      <SiteDrawer />
    </div>
  </body>
</html>
